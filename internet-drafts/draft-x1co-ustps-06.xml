<?xml version="1.0" encoding="UTF-8"?>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude"
     category="exp"
     docName="draft-x1co-ustps-06"
     ipr="trust200902"
     submissionType="independent"
     xml:lang="en"
     version="3">
  <front>
    <title abbrev="USTPS">UDP Speedy Transmission Protocol Secure (USTPS)</title>
    <seriesInfo name="Internet-Draft" value="draft-x1co-ustps-06"/>
    <author fullname="x1co" initials="X." surname="x1co">
      <organization>x1colegal</organization>
      <address>
        <email>x1colegal@outlook.com.br</email>
      </address>
    </author>
    <date year="2026" month="June" day="17"/>
    <area>General</area>
    <workgroup>Independent Submission</workgroup>
    <keyword>UDP</keyword>
    <keyword>streaming</keyword>
    <keyword>reliability</keyword>
    <keyword>security</keyword>
    <abstract>
      <t>This document describes UDP Speedy Transmission Protocol Secure (USTPS), an experimental transport built directly on UDP for low-latency and loss-tolerant applications. USTPS provides authenticated protection for DATA packets, readable plaintext control records authenticated by HMAC where applicable, binary UPACK DATA framing, selective retransmission, out-of-order acceptance, adaptive retransmission timeout, optional congestion control, and application-visible stream position metadata. USTPS is intentionally unordered at the transport layer and is designed to avoid transport-level Head-of-Line blocking.</t>
    </abstract>
  </front>
  <middle>
    <section numbered="true" toc="default">
      <name>Introduction</name>
      <t>USTPS is an experimental transport that runs directly over UDP. It was designed for applications that prefer continuing delivery of later packets even when earlier packets are temporarily missing. Current public implementations focus primarily on streaming, but the transport can also be adapted to other applications that either tolerate reordering or explicitly rebuild logical order at a higher layer.</t>
      <t>USTPS combines reliable recovery of missing packets, transport-level unordered behavior, and mandatory authenticated protection for application payload packets. Unlike TCP, USTPS does not force a single in-order byte stream in the transport itself. Unlike an individual QUIC stream, USTPS does not define in-order transport semantics inside the transport. Instead, it exposes both a packet sequence number and an application-facing stream position so that upper layers can reconstruct ordered output only when needed.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Terminology</name>
      <t>The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in BCP 14 <xref target="RFC2119"/> <xref target="RFC8174"/> when, and only when, they appear in all capitals, as shown here.</t>
      <t>This document uses the following terms:</t>
      <ul spacing="normal">
        <li><tt>seq</tt>: transport sequence number used for acknowledgement, loss detection, retransmission, and RTT measurement.</li>
        <li><tt>stream_pos</tt>: application-facing byte position indicating where a payload belongs in the logical ordered byte stream.</li>
        <li>UPACK: the binary USTPS DATA frame format, identified on the wire by the marker <tt>UPAK</tt>.</li>
        <li>Retry Token: the challenge token that proves reachability of the apparent source address before the server begins sending authenticated DATA.</li>
      </ul>
    </section>

    <section numbered="true" toc="default">
      <name>Design Goals</name>
      <t>USTPS has the following goals:</t>
      <ul spacing="normal">
        <li>Run directly over UDP.</li>
        <li>Provide authenticated protection for DATA packets.</li>
        <li>Recover missing packets with selective retransmission.</li>
        <li>Accept and process later packets even when earlier packets are missing.</li>
        <li>Expose sufficient metadata for an application to rebuild ordered output if required.</li>
        <li>Authenticate the retry-token handshake before the server starts sending encrypted DATA.</li>
        <li>Allow optional sender-side congestion control without changing the unordered transport model.</li>
      </ul>
      <t>USTPS is not intended to emulate TCP semantics. In particular, it does not provide transport-layer ordered byte-stream delivery. Optional congestion control, when implemented, changes only the sender rate policy and does not turn USTPS into a TCP-like ordered transport.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Transport Overview</name>
      <t>Each USTPS DATA packet contains a transport sequence number (<tt>seq</tt>) and a stream position (<tt>stream_pos</tt>). The sequence number is used for reliability functions such as acknowledgement, gap detection, retransmission, and RTT sampling. The stream position identifies where the payload belongs in the application's logical byte stream.</t>
      <t>Upon reception of DATA packets, a receiver SHOULD acknowledge unique packets and MAY immediately pass packet payload upward, even when a lower sequence number has not yet arrived. If an application requires ordered output, it MUST use <tt>stream_pos</tt> to reconstruct that order.</t>
      <t>The current public implementation uses a 32-bit monotonically increasing transport sequence space starting from 1 for each fresh session, and a 64-bit monotonically increasing <tt>stream_pos</tt> starting from 0 for each fresh logical stream. Sequence numbers are scoped to one session and are not meaningful across session resets.</t>
      <t>Example arrival order:</t>
      <artwork><![CDATA[
      Physical arrival: 1 2 3 5 6
      Missing packet : 4
      Receiver action: accept 5 and 6, request retransmission of 4
      Later arrival  : 4
      Logical order  : 1 2 3 4 5 6
      ]]></artwork>
    </section>

    <section numbered="true" toc="default">
      <name>Wire Formats</name>
      <t>USTPS uses three practical wire layers in current public implementations:</t>
      <ul spacing="normal">
        <li>plaintext transport control records</li>
        <li>binary DATA packets using UPACK</li>
        <li>an outer AEAD or cleartext+HMAC envelope for DATA packets</li>
      </ul>

      <section numbered="true" toc="default">
        <name>Plaintext Control Records</name>
        <t>Current public implementations serialize transport control packets as readable ASCII records. Typical forms include:</t>
        <ul spacing="normal">
          <li><tt>ACK: 10 MAC:&lt;tag&gt;</tt></li>
          <li><tt>ACK: 10 11 12 MAC:&lt;tag&gt;</tt></li>
          <li><tt>NACK: 42 MAC:&lt;tag&gt;</tt></li>
          <li><tt>NACK: 42 43 44 MAC:&lt;tag&gt;</tt></li>
          <li><tt>HELLO: BASE64...</tt></li>
          <li><tt>CLOSE:</tt></li>
        </ul>
        <t>ACK and NACK records remain plaintext for observability and debugging, but current implementations authenticate them with an HMAC derived from session state. The <tt>MAC:&lt;tag&gt;</tt> value is verified before the control record reaches the transport state machine.</t>
        <t>Current implementations batch up to 128 acknowledged or missing sequence numbers in one control record.</t>
      </section>

      <section numbered="true" toc="default">
        <name>UPACK DATA Format</name>
        <t>DATA packets use a binary frame format named UPACK, identified on the wire by the 4-octet marker <tt>UPAK</tt>.</t>
        <t>The UPACK DATA frame fields are:</t>
        <ul spacing="normal">
          <li>Magic: 4 octets</li>
          <li>Type: 1 octet</li>
          <li>Flags: 1 octet</li>
          <li>Sequence: 32 bits</li>
          <li>Stream Position: 64 bits</li>
          <li>Payload Length: 16 bits</li>
          <li>Payload: variable</li>
        </ul>
        <t>Packet types currently used by implementations include DATA, ACK, RETRANSMIT_REQUEST, HELLO, and CLOSE.</t>
        <t>In the current public implementation, the maximum UPACK payload is 1200 octets. The fixed UPACK header consumes 20 octets: 4 octets magic, 1 octet type, 1 octet flags, 4 octets sequence number, 8 octets stream position, and 2 octets payload length.</t>
      </section>

      <section numbered="true" toc="default">
        <name>Outer Secure Envelope</name>
        <t>Current public implementations protect DATA packets with an outer AEAD envelope identified by the magic value <tt>USS1</tt>, meaning "UDP Speedy Secure, version 1". After decryption, DATA typically begins with <tt>UPAK</tt>, while plaintext transport control remains readable ASCII.</t>
        <t>The current public implementation uses a 12-octet AEAD nonce and a 1-octet cipher identifier in the outer envelope. The nonce is generated randomly for each transmitted encrypted packet. Nonce uniqueness for a given key is mandatory; reuse of a nonce with the same key is forbidden.</t>
      </section>

      <section numbered="true" toc="default">
        <name>Outer Cleartext Envelope</name>
        <t>Current public implementations also support an OPTIONAL cleartext DATA mode identified by the magic value <tt>USC1</tt>, meaning "UDP Speedy Clear, version 1". In this mode, DATA payload bytes remain visible on the wire, but each DATA datagram carries an HMAC tag derived from session state.</t>
        <t>Cleartext mode provides integrity and tamper detection for DATA, but it does not provide confidentiality. Applications or deployments that require payload secrecy MUST use the AEAD envelope instead.</t>
      </section>
    </section>

    <section numbered="true" toc="default">
      <name>Datagram Size, MTU, and Fragmentation</name>
      <t>USTPS is designed to avoid relying on IP fragmentation. In the current public implementation, the transport payload limit is intentionally kept small enough to fit comfortably inside common Internet UDP paths.</t>
      <t>For the current public implementation, the effective encrypted DATA datagram is composed of:</t>
      <ul spacing="normal">
        <li>4 octets <tt>USS1</tt> envelope magic</li>
        <li>1 octet cipher identifier</li>
        <li>12 octets AEAD nonce</li>
        <li>20 octets UPACK fixed header</li>
        <li>up to 1200 octets DATA payload</li>
        <li>16 octets AEAD authentication tag for the currently supported ciphers</li>
      </ul>
      <t>This yields a maximum encrypted USTPS DATA payload of approximately 1253 octets before outer IP and UDP headers. With IPv4 and UDP headers, this is approximately 1281 octets on the wire; with IPv6 and UDP headers, approximately 1301 octets. This stays below the IPv6 minimum path MTU of 1280 only if the application payload is reduced somewhat, so implementations that require strict IPv6-minimum compliance SHOULD lower the application payload ceiling accordingly.</t>
      <t>In the current cleartext+HMAC mode, the envelope instead carries 4 octets of <tt>USC1</tt> magic, the UPACK packet, and a 16-octet HMAC tag. This yields approximately 1240 octets before IP and UDP headers, or approximately 1268 octets with IPv4+UDP and 1288 octets with IPv6+UDP.</t>
      <t>Current public implementations do not implement transport-layer fragmentation or transport-layer PMTU discovery. Instead, they use a fixed conservative payload ceiling. Applications or future versions MAY add explicit PMTU probing and dynamic payload sizing, but this is outside the current public implementation model.</t>
      <t>If the underlying IP layer fragments a datagram anyway, loss of any fragment causes the entire UDP datagram to be lost and later recovered through normal USTPS selective retransmission.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Handshake and Session Model</name>
      <t>A client begins by sending a plaintext HELLO carrying its X25519 public key, requested AEAD cipher, requested congestion-control mode, and requested DATA protection mode. The server does not send application DATA immediately. Instead, it replies with a plaintext challenge containing:</t>
      <ul spacing="normal">
        <li>a random retry token</li>
        <li>a generated Base64 session identifier</li>
        <li>the selected cipher</li>
        <li>the negotiated congestion-control mode</li>
        <li>the negotiated DATA protection mode</li>
        <li>the server public key</li>
      </ul>
      <t>The client MUST answer with the received token and matching session metadata. Only after that token round-trip succeeds does the server derive session state and begin sending authenticated DATA.</t>
      <t>The retry token proves reachability of the apparent source IP address and UDP port before the server starts sending encrypted payload data. In current public implementations, the resulting session is bound to the IP address and UDP source port that successfully completed the challenge. The session identifier remains useful as a label, but is not currently accepted from a different source address for path migration.</t>

      <section numbered="true" toc="default">
        <name>Implementation Flow</name>
        <t>A conforming implementation based on the current public model SHOULD behave roughly as follows:</t>
        <artwork><![CDATA[
        Client -> HELLO(client_pub, requested_cipher, requested_cc, requested_cleartext)
        Server -> CHALLENGE(token, session_id, selected_cipher, negotiated_cc, negotiated_cleartext, server_pub)
        Client -> CHALLENGE-REPLY(token, session_id, selected_cipher, negotiated_cc, negotiated_cleartext, client_pub)
        Server -> derive session key
        Server -> SESSION(session_id, selected_cipher, negotiated_cc, negotiated_cleartext, server_pub)
        Client -> derive session key
        Data path -> authenticated DATA packets begin
        ]]></artwork>
      </section>
    </section>

    <section numbered="true" toc="default">
      <name>Reliability Model</name>
      <t>USTPS uses selective retransmission. A sender stores transmitted packets in a retransmission buffer until they are acknowledged or otherwise retired. A receiver detects gaps in observed sequence numbers and requests retransmission only for missing packets.</t>
      <t>USTPS intentionally avoids Go-Back-N behavior. Later packets are not discarded solely because an earlier packet is missing. Retransmission timeout (RTO) is used as a fallback if explicit retransmission requests are delayed or lost. Current implementations adapt RTO from measured RTT samples taken from non-retransmitted acknowledged packets.</t>
      <t>Only the missing packets are retransmitted. Receipt of later packets does not require discarding or rewinding already accepted later packets.</t>

      <section numbered="true" toc="default">
        <name>Receiver Behavior</name>
        <t>A receiver implementation SHOULD:</t>
        <ul spacing="normal">
          <li>accept out-of-order DATA packets immediately</li>
          <li>track newly seen <tt>seq</tt> values to detect duplicates</li>
          <li>generate ACK for unique packets</li>
          <li>generate NACK only for missing sequence numbers</li>
          <li>retain enough metadata to reconstruct logical order by <tt>stream_pos</tt> if required by the application</li>
        </ul>
        <t>If a packet arrives with a <tt>seq</tt> that has already been accepted in the current session, it is a duplicate and SHOULD be ignored except for any minimal bookkeeping the implementation uses to refresh acknowledgement state. If a packet arrives with a very old <tt>seq</tt> that has already aged out of the receiver's recent history window, the implementation MAY ignore it silently.</t>
        <t>Current public implementations retain a bounded recent sequence history and periodically discard stale entries from that history. This prevents unbounded memory growth and limits the impact of delayed very-old packets from earlier portions of the session.</t>
      </section>

      <section numbered="true" toc="default">
        <name>Sender Behavior</name>
        <t>A sender implementation SHOULD:</t>
        <ul spacing="normal">
          <li>assign a unique increasing <tt>seq</tt> to each DATA packet</li>
          <li>store transmitted DATA packets in a retransmission buffer</li>
          <li>retire a packet from the retransmission buffer after successful acknowledgement</li>
          <li>retransmit only the missing packets identified by NACK or by RTO fallback</li>
        </ul>
        <t>If a packet has already been retired from the retransmission buffer, a later stale ACK or stale NACK for that packet MUST NOT recreate it artificially. Late control records referring to already-retired packets are therefore harmless and are simply ignored by the current public implementation.</t>
      </section>
    </section>

    <section numbered="true" toc="default">
      <name>Head-of-Line Blocking</name>
      <t>USTPS is designed to avoid transport-level Head-of-Line blocking. A missing packet does not prevent receipt, acknowledgement, buffering, or application visibility of later packets. This differs from TCP, where the transport enforces ordered byte-stream delivery, and from individual QUIC streams, which remain in-order within each stream.</t>
      <t>If an application chooses to emit only ordered output, any waiting that occurs is an application-layer decision, not a USTPS transport requirement.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Congestion Control</name>
      <t>USTPS Congestion is OPTIONAL in current implementations.</t>
      <t>When enabled, it adjusts the sender's effective send window and burst size without changing USTPS reliability semantics, unordered delivery semantics, or selective retransmission behavior.</t>
      <t>Current public implementations negotiate congestion-control mode during the handshake. Server policy may be <tt>auto</tt>, <tt>on</tt>, or <tt>off</tt>. Client preference may be <tt>on</tt> or <tt>off</tt>. With server policy <tt>auto</tt>, the server follows the client request. With server policy <tt>on</tt> or <tt>off</tt>, the server forces the final mode.</t>
      <t>Current public implementations start from normal transport speed and back off only after sustained congestion signals. Isolated loss events are intentionally tolerated so that a single loss event is less likely to be treated the same way as true persistent congestion.</t>
      <t>Current implementations examine a combination of measured RTT inflation, retransmission timeout events, and explicit retransmission pressure. Congestion control therefore affects only the sender rate policy; it does not alter the unordered transport model or the selective-repeat recovery model.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Security Model</name>
      <t>USTPS authenticates DATA packets in every supported mode. Implementations currently support AEAD protection with ChaCha20-Poly1305, AES-256-GCM, and AES-128-GCM, plus an OPTIONAL cleartext+HMAC mode for deployments that want visible payload bytes but still want tamper detection. A user-facing naming convention MAY map to those cipher suites as follows:</t>
      <ul spacing="normal">
        <li><tt>chacha20</tt> = <tt>CHACHA20_POLY1305</tt></li>
        <li><tt>aes-256-gcm</tt> = <tt>AES_256_GCM</tt></li>
        <li><tt>aes-128-gcm</tt> = <tt>AES_128_GCM</tt></li>
      </ul>
      <t>Current public implementations default to <tt>chacha20</tt> when a different explicit choice is not forced by configuration. In cleartext mode, the AEAD cipher negotiation remains part of handshake compatibility state, but DATA confidentiality is intentionally disabled and integrity is provided by HMAC.</t>
      <t>Clients and servers perform an X25519-based key exchange when a client joins, and a separate session key is derived per client session.</t>
      <t>ACK and NACK records remain plaintext in current implementations for observability and debugging, but they are authenticated with an HMAC derived from the session key. This is intended to reduce off-path forged ACK or NACK attacks while preserving readable control format.</t>
      <t>Implementations described by this document use a persistent server host key and Trust On First Use (TOFU) on the client side to detect unexpected server key changes across reconnects.</t>
      <t>The retry token is a random reachability challenge value. It is not reused as an AEAD key, not reused as an HMAC key, and not reused as a packet nonce. Its role is limited to proving that the apparent source endpoint can receive and return server-issued challenge material before the server begins sending encrypted DATA.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Session Binding and Network Changes</name>
      <t>Earlier experimental implementations attempted client path migration and session resumption across source IP address or UDP port changes. Current public implementations removed that behavior because it created practical reliability and security problems, including migration floods, stale sessions that appeared recovered but stopped delivering data, long silent periods followed by gap-only behavior, and ambiguity between a legitimate roaming client and spoofed packets claiming an existing session identifier.</t>
      <t>Current public implementations therefore use a simpler model: complete the retry-token challenge on the current source IP address and UDP port, bind the resulting session to that endpoint, and reconnect cleanly if the endpoint changes.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Implementation Guidance</name>
      <t>An implementation that needs ordered application output SHOULD keep a reorder buffer keyed by <tt>stream_pos</tt>, not by <tt>seq</tt>. The sequence number exists for transport reliability logic; it is not the application's authoritative logical ordering key.</t>
      <t>An implementation that can consume unordered chunks directly MAY process payloads immediately without ordered release.</t>
      <t>Applications that bridge USTPS into a TCP-like local compatibility socket should understand that any ordered byte stream visible on that local side is an application choice above USTPS, not transport-level ordering inside USTPS.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Operational Notes</name>
      <t>USTPS has been implemented and exercised as a Beta-grade experimental protocol. Current public code and testing are primarily focused on streaming over local networks and over public Internet paths where UDP is available. Current implementations are multi-client and perform per-client session key derivation.</t>
      <t>Current public implementations also expose knobs such as simulated packet loss, output buffering delay, cipher selection, DATA protection mode selection, stream container choice, and optional congestion-control policy. These are implementation details and not normative wire requirements unless later specified.</t>
    </section>

    <section numbered="true" toc="default">
      <name>IANA Considerations</name>
      <t>This document has no IANA actions.</t>
    </section>

    <section numbered="true" toc="default">
      <name>Security Considerations</name>
      <t>USTPS relies on correct AEAD use where confidentiality is enabled, correct HMAC validation where cleartext mode is enabled, correct nonce handling, correct key derivation, authenticated peer identity checks, and correct validation of the retry-token challenge before authenticated DATA is sent. Implementations MUST ensure nonce uniqueness for a given key. Implementations SHOULD authenticate server identity changes using a trust model such as TOFU or a stronger out-of-band validation method.</t>
      <t>Because USTPS is unordered, applications that require ordered interpretation MUST reconstruct order using application-visible metadata such as <tt>stream_pos</tt>. Applications that incorrectly treat physical arrival order as logical order may mis-handle retransmitted data.</t>
      <t>Because current public implementations keep ACK and NACK records in plaintext, implementations that follow this model SHOULD authenticate those control records, for example with an HMAC derived from session state.</t>
      <t>Deployments using cleartext+HMAC mode MUST understand that this mode does not hide application payload bytes from observers. It protects integrity, not confidentiality.</t>
      <t>Implementations that expose optional congestion control SHOULD avoid treating an isolated single loss event as automatically equivalent to sustained path congestion.</t>
      <t>Because current public implementations do not perform transport-level PMTU discovery, operators deploying USTPS across unusual tunnels or paths with reduced MTU may need a smaller application payload ceiling to avoid excessive IP fragmentation.</t>
    </section>
  </middle>
  <back>
    <references>
      <name>Normative References</name>
      <reference anchor="RFC2119" target="https://www.rfc-editor.org/info/rfc2119">
        <front>
          <title>Key words for use in RFCs to Indicate Requirement Levels</title>
          <author initials="S." surname="Bradner" fullname="Scott Bradner"/>
          <date year="1997" month="March"/>
        </front>
        <seriesInfo name="BCP" value="14"/>
        <seriesInfo name="RFC" value="2119"/>
      </reference>
      <reference anchor="RFC8174" target="https://www.rfc-editor.org/info/rfc8174">
        <front>
          <title>Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words</title>
          <author initials="B." surname="Leiba" fullname="Barry Leiba"/>
          <date year="2017" month="May"/>
        </front>
        <seriesInfo name="BCP" value="14"/>
        <seriesInfo name="RFC" value="8174"/>
      </reference>
    </references>
  </back>
</rfc>
